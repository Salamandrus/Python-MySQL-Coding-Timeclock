    # COMMENTS #
    # PYTHON CODE START #

import mysql.connector

mydb = mysql.connector.connect(host='localhost', user='root', password='Wyvern42!', database='testdb')

mycursor = mydb.cursor()

mycursor.execute(
    ""
)
    # PYTHON CODE END #

    # SQL CODE START #

    # BUTTON 1 - CONFIGURE CONNECTION #
Create code which modifies my.sql.connector.connect(host='', user='', password='', database='') based on input()

    # BUTTON 2 - NEW DATABASE #
CREATE DATABASE input('Enter new database name here: ')

    # BUTTON 3 - SHOW DATABASES #
SHOW DATABASES
print

    # BUTTON 4 - NEW USER #
CREATE TABLE tablename (
id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
DATE,
TIMESTAMP,
Location VARCHAR(30),
Punch In VARCHAR(5),
Punch Out VARCHAR (5),
)
    # BUTTON 5 - VIEW USERS #
SHOW TABLES
print

    # BUTTON 6 - VIEW TIMESTAMPS #
    # BUTTON 7 - PUNCH IN #
INSERT INTO table_name
VALUES (id, CURRENT_DATE, CURRENT_TIMESTAMP, 'input('Enter location: '), 'True', 'False')'
    # BUTTON 8 - PUNCH OUT #
INSERT INTO table_name
VALUES (id, CURRENT_DATE, CURRENT_TIMESTAMP, 'input('Enter location: '), 'False', 'True')'
    # BUTTON 9 - ABOUT #
print README

    # SQL CODE END #

    # COMBINED CODE START #



    # COMBINED CODE END #
